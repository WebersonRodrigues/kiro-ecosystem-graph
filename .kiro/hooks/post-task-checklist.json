{
  "name": "Post-Task Checklist",
  "version": "1.0.0",
  "description": "Verifies quality after each spec task completion.",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Checklist before marking task complete: 1) getDiagnostics on ALL edited files? 2) Tests created if required? 3) Backward compatibility verified? 4) Security ok per security-policies.md? 5) Code standards per code-conventions.md followed? If all pass: silence. If any failed: fix NOW."
  }
}
